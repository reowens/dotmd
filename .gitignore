node_modules/

# OS
.DS_Store
Thumbs.db

# Editor / IDE
*.swp
*.swo
*~
.idea/
.vscode/
*.code-workspace

# Environment & secrets
.env
.env.*
.npmrc

# Claude Code local settings
.claude/settings.local.json
.claude/scheduled_tasks.lock

# Build / cache
.osgrep
.gmax
.gmax.json
*.tgz

# Lockfiles from other package managers (this is an npm project)
pnpm-lock.yaml
yarn.lock
.dotmd/

# Live saved-prompt queue — session-local, never committed. The single `*` stops
# at the directory on purpose: docs/prompts/archived/ is the committed record.
/docs/prompts/*.md
